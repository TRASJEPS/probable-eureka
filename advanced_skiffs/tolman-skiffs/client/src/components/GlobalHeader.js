// USE the CONTEXT AS A CONSUMER THIS IS FOR USER GLOBAL CONTEXT
// Providers hold onto the piece and the consumer is what needs it

import { useContext } from "react";
import React, { useContext } from 'react';
